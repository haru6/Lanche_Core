@model SnackListViewModel

<div>
    <h2>@Model.CategoryCurrent</h2>
</div>
<div class="form-row">
    @foreach (var snack in Model.Snacks)
    {
        <partial name="_SnackResume" for="@snack" />
    }
</div>